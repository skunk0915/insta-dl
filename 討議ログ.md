# 討議ログ

## 2026-07-22 08:20:00
- ユーザーより「geminiが行った直近の改修（画質改善）で、画質以前に動画がまともにDL出来なくなった」との指摘。
- 経緯を chat.txt / app.log / 本番サーバー(sakura)の実ログを確認して整理：
  1. 画質改善のため `format: 'bestvideo+bestaudio/best'` に変更 → ローカル(ffmpeg有)ではOKだが、本番さくらサーバーにffmpegが無く「ffmpeg is not installed」エラーでDL自体が全滅する状態になっていた。
  2. 応急対応として、ffmpeg不在時は `best`（単一ファイル・低画質）へ自動フォールバックする実装に変更 → DLは復旧したが画質改善の目的は未達成。
  3. ユーザーから「サーバーで動かないと意味がない」という指摘（フィードバック）を受け、さくらサーバー(FreeBSD 13.0)向けに静的ビルドのffmpegをサーバー上でコンパイルし `bin/ffmpeg` に配置。これによりサーバー上でも高画質マージが機能する状態にした。
- 本セッションで本番API (`/api/download`)を直接curlで叩いて再現テストを実施 → 現時点では200 OKで成功し、取得したmp4は 1080x1920 / VP9 / 約5.8Mbps と高画質マージが機能していることを確認。
- 未解決事項：ユーザーが「まともにDL出来ない」と感じた具体的なタイミング・URL・エラー文言が不明。現状のテストでは再現しなかったため、ユーザーに再現状況の詳細を確認中。
- 学び：サーバー環境依存のバイナリ（ffmpeg等）を要求する変更は、必ず本番相当の環境でも動作確認してからでないと「ローカルでは直った」が本番で全滅する事故につながる。今後同様の改修時は本番環境の依存関係（ffmpeg等の外部バイナリ）を先に確認する。
